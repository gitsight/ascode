name: 'ascode-action'
description: 'AsCode - Terraform Alternative Syntax'
inputs:
  file:  
    description: 'starlark file to execute'
    required: true
    default: 'main.star'
  hcl:  
    description: 'hcl output file'
    required: true
    default: 'generated.tf'
  pluginDir:
    description: 'path to plugin dir'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
